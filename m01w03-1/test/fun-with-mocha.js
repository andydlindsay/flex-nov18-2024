// describe(); // optional; grouping related tests together

// it(); // actual test

describe('just experimenting', () => {

  it('fails on error', () => {
    // throw new Error('ooops! bad stuff happened');
  });

});
